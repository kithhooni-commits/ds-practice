"""[폐기] run_day3.py 로 대체됨.

이 스크립트는 K 와 λ 를 **test 에서** 스윕했다. 학습은 아니지만 하이퍼파라미터를
test 로 고른 것이라 test 를 쓴 셈이다. 같은 비교를 val 에서 튜닝하고 test 로 채점하는
run_day3.py 를 쓸 것.
"""

import sys

print(__doc__)
sys.exit(1)
